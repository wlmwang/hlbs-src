
/**
 * Copyright (C) Anny.
 * Copyright (C) Disvr, Inc.
 */

#ifndef _W_DISPATCH_H_
#define _W_DISPATCH_H_

#include <string>
#include <map>
#include "wType.h"

/*
class wDispatch
{
	public:
		wDispatch();
		~wDispatch();
		void Register(string className, wDispatch *pDispatch);
		void init();
		void Dispatch(string className, string actionName, ...);
	protected:
		map<string,wDispatch*> mDispatch;
};

void Register(string className, wDispatch *pDispatch)
{
	mDispatch.insert(pair<string,wDispatch*>(className, pDispatch));
}

void init()
{
	if(map<string,wDispatch*>::iterator it = mDispatch.begin();it != mDispatch.end(); it++)
	{
		it->second()->init();
	}
}
*/


#endif