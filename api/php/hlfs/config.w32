// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("hlfs", "for hlfs support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("hlfs", "enable hlfs support", "no");

if (PHP_HLFS != "no") {
	EXTENSION("hlfs", "hlfs.c");
}

